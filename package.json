{
  "name": "skribble",
  "version": "1.0.0",
  "description": "guess a drawing game",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "startClient": "cd client && npm i && npm start",
    "startServer": "cd server && npm i && npm start",
    "start": "npm-run-all --parallel startServer startClient"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/0608Santosh/skribbl.git"
  },
  "author": "santosh",
  "homepage": "https://github.com/0608Santosh/skribbl#readme",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
